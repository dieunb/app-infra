apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kong-ingress
  namespace: devops
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
  - http:
      paths:
      - path: /kong
        backend:
          serviceName: kong-gateway-kong-admin
          servicePort: 8001

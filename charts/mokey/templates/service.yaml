apiVersion: v1
kind: Service
metadata:
  name: {{ include "mokey.fullname" . }}
  labels:
    {{- include "mokey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mokey.selectorLabels" . | nindent 4 }}
